pull_request_rules:
  - name: Automatically merge pull requests on approval ✅
    conditions:
      - or:
          - "#approved-reviews-by>=1"
          - author=hllywluis
      - and:
          - check-success=build
          - check-success=Analyze
    actions:
      merge:
        method: merge
      delete_head_branch:
